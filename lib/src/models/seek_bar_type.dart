/// Defines the type of seek bar to render.
enum SeekBarType {
  /// A standard single-thumb linear slider.
  single,

  /// A two-thumb range linear slider (min/max).
  range,

  /// A circular arc slider with a single thumb.
  circular,
}
